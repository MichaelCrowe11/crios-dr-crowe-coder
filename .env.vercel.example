# Vercel Environment Variables
# These should be configured in Vercel Dashboard: https://vercel.com/dashboard/[project]/settings/environment-variables

# ============================================
# Public Environment Variables (Build Time)
# ============================================
# These are embedded in the frontend bundle at build time

# API endpoints - for Vercel deployment, API routes are on same domain
NEXT_PUBLIC_API_URL=https://your-project.vercel.app
NEXT_PUBLIC_WS_URL=wss://your-project.vercel.app

# ============================================
# Server-Side Environment Variables
# ============================================
# These are only available in API routes and server-side code

# Database - Use Vercel Postgres or external provider
# Option 1: Vercel Postgres (recommended)
POSTGRES_URL="postgres://default:***@***-pooler.us-east-1.postgres.vercel-storage.com:5432/verceldb"
POSTGRES_PRISMA_URL="postgres://default:***@***-pooler.us-east-1.postgres.vercel-storage.com:5432/verceldb?pgbouncer=true&connect_timeout=15"
POSTGRES_URL_NON_POOLING="postgres://default:***@***-pooler.us-east-1.postgres.vercel-storage.com:5432/verceldb"
POSTGRES_USER="default"
POSTGRES_PASSWORD="***"
POSTGRES_DATABASE="verceldb"

# Option 2: External PostgreSQL (Neon, Supabase, etc.)
# DATABASE_URL="postgresql://user:password@host:5432/database"

# Redis - Use Upstash Redis (recommended for Vercel)
# Get from: https://console.upstash.com/
REDIS_URL="redis://default:***@***upstash.io:6379"
UPSTASH_REDIS_REST_URL="https://***.upstash.io"
UPSTASH_REDIS_REST_TOKEN="***"

# AI API Keys
ANTHROPIC_API_KEY="sk-ant-***"

# Application Settings
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# Security
# Generate with: openssl rand -base64 32
SECRET_KEY="***"
NEXTAUTH_SECRET="***"
NEXTAUTH_URL="https://your-project.vercel.app"

# ============================================
# Optional: Background Jobs (Vercel Cron or QStash)
# ============================================

# Vercel Cron (for scheduled tasks)
# Configure in vercel.json with crons array

# QStash (for queue-based background jobs)
# Get from: https://console.upstash.com/qstash
QSTASH_URL="https://qstash.upstash.io"
QSTASH_TOKEN="***"
QSTASH_CURRENT_SIGNING_KEY="***"
QSTASH_NEXT_SIGNING_KEY="***"

# ============================================
# How to Set These in Vercel
# ============================================
# 1. Go to: https://vercel.com/dashboard
# 2. Select your project
# 3. Settings → Environment Variables
# 4. Add each variable with appropriate scope:
#    - Production (for production deployments)
#    - Preview (for PR previews)
#    - Development (for local development with `vercel dev`)
#
# For sensitive values, use Vercel's encrypted storage
# For Vercel Postgres and Upstash Redis, use the integrations:
#   - Storage → Connect Store → Postgres
#   - Integrations → Add Integration → Upstash Redis

# ============================================
# Quick Setup with Vercel CLI
# ============================================
# Install Vercel CLI: npm i -g vercel
#
# Link project:
# vercel link
#
# Add environment variables:
# vercel env add ANTHROPIC_API_KEY
# vercel env add POSTGRES_URL
# vercel env add REDIS_URL
#
# Pull environment variables for local development:
# vercel env pull .env.local
